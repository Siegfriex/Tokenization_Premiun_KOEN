# G1 Decision Queue — v1

**Status (2026-08-16, canonical `research/g1-claude` freeze): no Research Director decision remains open for D-01 implementation.** Every item below that could be resolved by an approved decision, by SSOT text, or by L4-verified evidence has been. What's left is genuinely unresolvable at the contract-design stage — it requires either an external official source, further data investigation, or Phase 2 execution, not another Director ruling.

## Remaining unresolved (only genuine open items)

| Item | Nature | What would close it |
|---|---|---|
| **Official provenance closure** — `provenance_closure_status` for 025/026 (`PENDING_OFFICIAL_SCHEMA_AND_RELEASE_LINK`) and Legacy (`PARTIAL_OFFICIAL_CONSTRUCTION_CONFIRMED_FIELD_AND_RELEASE_LINK_PENDING`) | External-evidence gap | An official AIHub schema/version/release-link confirmation (WEB-track evidence) matching the local corpora — not a Director decision |
| **Legacy composition interpretation** — is D87's local Legacy XLSX corpus actually a single coherent dataset, or should 뉴스/조례/대화체/etc. be treated as separable sub-sources; and root cause of the News(2)↔한국문화 2,469-pair overlap (`POTENTIAL_SOURCE_REUSE/COMPOSITION_OVERLAP`) | Data-investigation gap | Further data-recon/Codex investigation into why two differently-domain-labeled workbooks share so many exact pairs, and an official D87 construction document if one becomes available |
| **Later Phase 2 semantic QC outcomes** — actual `pair_quality_score` values, real accepted/review/rejected counts, the realized final cohort N (per `primary_cohort_policy`), and the residual ~46% of 025's duplicate groups not explained by cross-direction mirroring (§7 of the pair registry precontract) | Downstream execution, not a design gap | Running `02_normalize_and_qc` and the semantic-alignment measurement it requires — none of this can be decided in advance at contract-freeze time |

Everything below this line is the resolution history — kept for traceability, not because it's still open.

## Resolution history

| Item | Classification | Rationale |
|---|---|---|
| AMB-05 — final analysis N | **`RESOLVED_POLICY (NUMERIC_N_POST_QC)`** — D-RD-08 | No longer "pick a target N." Primary cohort = all 025+026 pairs that are G1 ingest-valid + G2/QC-accepted + pass exact-duplicate policy + Tier-A eligible. Final numeric N is a *realized value* of that pipeline, not chosen in advance. No arbitrary cap, no pre-analysis 100k sampling, no 500k convenience cap. SSOT's 100,000+ is a recommended floor, not a ceiling. The actual number is not written anywhere yet — it emerges after QC/dedup runs. |
| AMB-12 — source tier ↔ corpus mapping | **`RESOLVED`** — D-RD-05 | 025=Tier A/`PRIMARY_BACKBONE`; 026=Tier A/`PRIMARY_DOMAIN_SUPPLEMENT` (never pooled with 025 without an explicit stratum label); Legacy=**`source_tier=null/UNASSIGNED`**, `research_role=SENSITIVITY_ONLY`, `primary_analysis_eligible=false` (the earlier Tier-B-tentative recommendation is **withdrawn** — Tier B must not be used as a provenance-shortfall fallback); D71693=`NOT_ACQUIRED/OUTSIDE_CURRENT_CORPUS`. Not yet written into `configs/research_v1.yaml` — see PROPOSED_G1_CONFIG_DELTA in the pair registry precontract. |
| Duplicate Analysis-Representative selection rule (which record wins within a `duplicate_group_id`) | **`RESOLVED`** (finalized 2026-08-16 — targeted EDA/recon judged sufficient) | `representative_pair_id = lexicographically minimum pair_id within duplicate_group_id`, deterministic, no semantic preference (train/validation, direction, domain, source, tier, text length all explicitly forbidden as selection criteria). See `G1_PAIR_REGISTRY_PRECONTRACT_v1.md` §7. |
| Train/validation overlap handling (keep AIHub's upstream split vs rebuild via LR-01 grouping) | **`RESOLVED_BY_SSOT_LR01`** (reclassified — no longer a Director decision) | AIHub's train/valid label is preserved only as `is_validation_upstream` (upstream provenance metadata). The project's own predictive split is built independently per LR-01 (duplicate/near-duplicate groups placed on one side) — the question "do we trust AIHub's split" doesn't need separate adjudication once LR-01 is applied as SSOT already requires. |
| `pair_id` = provenance-derived (not content hash) design | **`RESOLVED`** — Vice Director accepted (no longer proposal) | Binding: Record Identity ≠ Pair Content Identity; `pair_id`=provenance-derived, `duplicate_group_id`=content-derived (direction-agnostic); see `PAIR_IDENTITY_AND_DUPLICATE_CONTRACT_v1.md` status banner. |
| Legacy `source_record_id` mechanism (workbook+sheet+physical_row fallback) | `ENGINEERING_DETAIL` (unchanged) | Forced by evidence — no reliable natural key exists in any Legacy workbook (127,824 duplicate ID/SID rows); implementation is Codex's |
| Direction-confidence handling rule | `CAN_BE_RESOLVED_BY_SSOT` (unchanged in classification; **value corrected**) | **D-RD-06 correction**: Legacy → `UNKNOWN`, **not** `HUMAN_PARALLEL_UNKNOWN` (current evidence does not confirm human-parallel construction; upgrade only if official documentation later confirms it). 025/026 rules (contradiction→`UNKNOWN`, weak-signal→review flag) unchanged. |
| Domain mapping — 한국문화 (`general` vs `other`) | **`RESOLVED`** — D-RD-07 | → `other` |
| Domain mapping — 조례/ordinances (`legal` vs `administration`) | **`RESOLVED`** — D-RD-07 | → `legal` |
| Domain mapping — all other top-level categories | **`RESOLVED`** — D-RD-07 (see full table in `DIRECTION_AND_DOMAIN_MAPPING_PRECONTRACT_v1.md`) | 025: 일상생활→general, 해외고객과의채팅→dialogue, 해외영업→other. 026: 기술과학→technology, 세계/경제/정치/기후→other. Legacy: 구어체→general, 대화체→dialogue, 뉴스→news, 지자체웹사이트→administration. **Subdomains remain raw-only by design, not pending.** |
| Auxiliary D-01 fields (`source_record_id`, `raw_locator`, `duplicate_group_id`, `domain_raw`, `subdomain_raw`, `is_validation_upstream`, etc.) | `ENGINEERING_DETAIL` (unchanged — the `pair_id` design is now resolved, so this follows) | Mechanical consequences of the identity design |
| 025 duplicate-mechanism hypothesis (TL1/TL2 direction-mirroring vs accidental repetition) | **`PARTIALLY_RESOLVED`** — residual folded into "Remaining unresolved" §Phase-2 outcomes above | Cross-direction overlap = 50,511 of 93,823 total duplicate groups (~54%) confirms direction-mirroring is real and material. The remaining ~46% (≈43,312 groups) is *not* cross-direction and is now tracked as part of Phase-2/downstream execution, not a design gap |
| Legacy News(2)↔한국문화 exact-pair overlap (2,469, L4-verified) | folded into "Remaining unresolved" §Legacy composition interpretation above | Recorded as `POTENTIAL_SOURCE_REUSE / COMPOSITION_OVERLAP`, not asserted as an error; root cause routed to data-recon/Codex |
| Group-resolved metadata rule beyond `translation_direction` (e.g. conflicting `domain`/`source_id` within one `duplicate_group`) | `ENGINEERING_DETAIL` pending a concrete conflict case — not yet observed | The directive specifies a concrete resolution rule only for `translation_direction`; generalizing it is a follow-on design question, not urgent absent an observed conflict |
| Raw `source` field (SBS/크라우드소싱/한국연구재단/특허정보원 etc.) vs canonical `source_id` | **`RESOLVED`** — new distinction (directive §4) | Raw values are `source_provenance_raw`, an auxiliary field — never the canonical `source_id` (which is corpus/acquisition-level: 025-family/026-family/Legacy-family). See `G1_PAIR_REGISTRY_PRECONTRACT_v1.md`. |
| Identifiability Gate minimum diagnostics | **`RESOLVED`** — now required (directive §5) | `source_id×domain`, `source_id×direction`, `raw_source×domain`, `raw_source×direction` cross-tabs are mandatory before any independent-effect interpretation; 026's `domain=기술과학`≡`source=특허정보원` near-perfect confound (confirmed via `eda/g0-raw-notebooks` category counts) is the concrete evidence motivating this. |
| 026 scale discrepancy (local 1.35M vs web "1.5M sentences") | *(not escalated)* | Logged in the reconciliation doc as a minor, non-blocking discrepancy |

## Section 13 — Perplexity SSOT Amendment Review

**Proposed amendment** (from `origin/evidence/g0-perplexity`): add a clause stating "AI Hub KO-EN is not a single corpus identifier," requiring each result to report official dataset ID/title/URL/access date/archive SHA-256/selected fields/direction/normalization/filtering/final N/acquisition constraint, and treating datasets as separate strata absent a documented pooling justification.

**Verdict: `NO_SSOT_CHANGE_REQUIRED`** — this is operational/data-contract elaboration, not a change to SSOT's research meaning. Every element Perplexity asks for is already expressible inside the existing contract:
- "not a single corpus identifier" / "separate strata unless pooling is justified" ≈ SSOT §20.1 (source fixed/random effects), §20.2 (Identifiability Gate), T-04 (domain-source confounding) — already forbids silently pooling sources without identifiability checks, and this reconciliation's own Task 2 already assigns 025/026/Legacy as distinct roles rather than one pooled "AIHub" blob.
- official dataset ID/title/URL/access date/archive SHA-256/acquisition constraint ≈ D-01's `source_id`, `source_license_note` fields (§12.1) plus §30.2's lineage requirement (raw file hash, source registry snapshot, acquisition metadata) — already operationalized in `docs/contracts/G1_PAIR_REGISTRY_PRECONTRACT_v1.md` §1-2.
- selected KO/EN fields, translation direction, normalization/filtering policy, final paired N ≈ already covered by §11 (normalization), §9.2 (translation_direction), §10 (QC), and this decision queue's own open items above.

**Conclusion**: adopt Perplexity's request as **elaboration of how the existing source registry is documented and populated**, not as a new SSOT clause. No CR is proposed for this item.
